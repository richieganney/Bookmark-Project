CREATE TABLE bookmarks(id SERIAL PRIMARY KEY, url VARCHAR(60));
-- above is for bookmark_manager db

CREATE TABLE bookmarks(id SERIAL PRIMARY KEY, url VARCHAR(60)); 
-- above is for bookmark_manager_test db
